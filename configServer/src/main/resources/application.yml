spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/integraceferina/esp-back-i-parcial
          default-label: develop
          force-pull: true
          search-paths: config-data

server:
  port: ${PORT:8888}



